frappe.listview_settings["Constituency Entry"] = {
    /* add_fields: ["province"],
    filters: [
        ['province', '=', 'Southern']
    ], */

    /*add_fields: ["province", "user_type", "user_image"],
	filters: [["province", "=", "Southern"]],
	prepare_data: function (data) {
		data["user_for_avatar"] = data["name"];
	},
	get_indicator: function (doc) {
		if (doc.province == "Southern") {
			return [__("Active"), "green", "enabled,=,1"];
		} else {
			return [__("Disabled"), "grey", "enabled,=,0"];
		}
	},*/
}